// const jwt = require("jsonwebtoken");

// module.exports = () => {
//   const access_token = jwt.sign({}, { expiresIn: "2d" });
//   const refresh_token = jwt.sign({}, { expiresIn: "30d" });

//   return {
//     access_token,
//     refresh_token,
//   };
// };
